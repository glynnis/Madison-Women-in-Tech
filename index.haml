---
layout: default
h1: Hi, I'm Glynnis.
---

.about-text
  {% for post in site.posts limit: 10 %}
  %h2 {{ post.title }}
  .post-date
    {{ post.date | date_to_long_string }}
  %p {{ post.content }}
  %p.post-permalink
    %a{:title => "{{ post.title }}", :href => "{{ post.url }}"} Permalink
  {% endfor %}
